#include <stdio.h>
int main()
{
    int a;
     int remainder;
    printf("Enter a positive integer: ");
    scanf("%d", &a);
    int i=a;
    while (i>=0)
    {
		int sum=0;
    
    remainder=a%10 
    sum = remainder + sum*10;
   }
		printf("Sum = %d", sum);

    return 0;
}
