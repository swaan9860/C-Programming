#include <stdio.h> 
#include <conio.h>

int main()
{
int length, breadth, area, perimeter ; 
printf ("enter length and breadth: \n");
scanf ("%d", & length); 
scanf ("%d",& breadth);
area: (length*breadth);
perimeter = 2* (length + breadth); 
printf ("area is :%d", area);
printf ("perimeter its :%d", perimeter);

return 0;
}
